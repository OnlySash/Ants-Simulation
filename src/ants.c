#include <pthread.h>
#include <stdio.h>
#include <stdlib.h>
#include <unistd.h>


void ant_creation(){
      //t_nu, cantidad de hilos
  //pthread_t t_ids[t_num];
  //pthread_mutex_init(&mutex, NULL); 

  // Crear cada hilo en simultaneo
  //for (int t = 0; t < t_num; ++t) {
    //data *t_data = malloc(sizeof(data));

    //t_data->array = arr;

    //pthread_create(&t_ids[t], NULL, ant_creation, NULL);
  //}

  //for (int t = 0; t < t_num; ++t) {
    //pthread_join(t_ids[t], NULL);
  //}
}